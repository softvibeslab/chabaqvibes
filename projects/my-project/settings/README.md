# Settings

Place Settings 2.0 Monaco configuration files here.

Typical files:

```text
settings/
├── config.yaml
└── template.json
```

Use this folder for Dynatrace settings schemas that should be managed as code.

